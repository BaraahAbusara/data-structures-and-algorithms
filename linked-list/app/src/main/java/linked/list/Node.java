package linked.list;

public class Node<T> {

        T value ;
        Node nextNode;
        Node previousNode ;
        public Node(T value)
        {
            this.value=value;
        }
}
