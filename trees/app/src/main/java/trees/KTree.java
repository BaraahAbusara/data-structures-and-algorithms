package trees;

public class KTree {
    NodeK <Double> root;
    int k ;

    public KTree(int k) {
        this.k = k;
    }
    public void add(double value){
        //add a node to the tree

    }
    public KTree fizzBuzz(KTree tree){
        KTree newTree = tree;

        //I want to traverse through the k-ary tree and edit each node's value one by one.

        return newTree;

    }

    Boolean isEmptyTree (){
        return root == null;
    }
}
